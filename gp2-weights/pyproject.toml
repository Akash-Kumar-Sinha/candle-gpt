[project]
name = "gp2-weights"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.14.0",
    "transformers>=5.17.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
